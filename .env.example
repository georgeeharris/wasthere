# Environment variables for WasThere application
# Copy this file to .env and customize as needed

# IMPORTANT: Change these credentials before deploying to production!

# Database Configuration
POSTGRES_DB=wasthere
POSTGRES_USER=wasthere_user
POSTGRES_PASSWORD=change_this_password_in_production

# API Configuration
ASPNETCORE_ENVIRONMENT=Production

# CORS Configuration
# Comma-separated list of allowed origins for CORS
# For production, set this to your actual domain(s)
# Example: CORS_ORIGINS=http://yourdomain.com,https://yourdomain.com
CORS_ORIGINS=http://www.wasthere.co.uk,https://www.wasthere.co.uk,http://www.wasthere.co.uk:5000,https://www.wasthere.co.uk:5000,http://localhost,http://localhost:5173,http://localhost:3000,http://localhost:5000

# Frontend Configuration (for build time)
# For production, set this to your actual domain (NOT an IP address to avoid CORS issues)
# Using the domain name ensures CORS works correctly between frontend and API
# Use https:// if SSL/HTTPS is enabled
VITE_API_URL=http://www.wasthere.co.uk:5000/api
# For HTTPS, change to:
# VITE_API_URL=https://www.wasthere.co.uk:5000/api

# Google Gemini API Configuration
# TODO: For production, set this via environment variable instead of appsettings.json
# GOOGLE_GEMINI_API_KEY=your_api_key_here
# Auth0 Configuration
# These values are PUBLIC for Single Page Applications (SPAs)
# Security comes from the Allowed Callback URLs whitelist in Auth0 dashboard
AUTH0_DOMAIN=dev-z5oejemhc2qshuiy.uk.auth0.com
AUTH0_AUDIENCE=https://wasthere-api
AUTH0_CLIENT_ID=jfxFjD5cancaXhCIWSf1536edTCKMVvx
# Auth0 Configuration
# Domain from your Auth0 tenant (e.g., your-tenant.auth0.com)
AUTH0_DOMAIN=your-tenant.auth0.com
# API Identifier/Audience from Auth0 API settings
AUTH0_AUDIENCE=https://wasthere-api
# Client ID from Auth0 Application settings (for frontend)
AUTH0_CLIENT_ID=your_client_id_here

# Port Configuration (optional - defaults shown)
# DB_PORT=5432
# API_PORT=5000
# WEB_PORT=80

# HTTPS/SSL Configuration (optional)
# Uncomment and configure these if you want to enable HTTPS
# See HTTPS-SETUP.md for detailed instructions
# WEB_HTTPS_PORT=443
# ENABLE_HTTPS=true
# SSL_CERT_PATH=/etc/letsencrypt/live/www.wasthere.co.uk/fullchain.pem
# SSL_KEY_PATH=/etc/letsencrypt/live/www.wasthere.co.uk/privkey.pem
